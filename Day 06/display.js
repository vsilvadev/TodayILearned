//Things that we can use do display information in JS 

let init = "hello";

const human = {
    name: "Vitor",
    age: 19,
    weight: 78.5,
};

console.log(init);//Displays a message on the console
console.warn(init);//Write an alert message on the Console (browser)
console.table(human);//Displays data as a table.

