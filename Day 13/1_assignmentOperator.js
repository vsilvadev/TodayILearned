const a = 7; //Assigning a value of 7 to the Var
let b = 3; //Assigning a value of 3 to the Let

b += a; //b = b + a
console.log(b);

b -= 4; //b = b - 4
console.log(b);

b *= 2; //b = b * 2
console.log(b);

b /= 2; //b = b / 2
console.log(b);

b %= 2; //b = b % 2
console.log(b);
